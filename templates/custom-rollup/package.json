{
  "name": "custom-rollup",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A QoreChain custom rollup (optimistic / dedicated / native / standard / EVM, fully overridable) demonstrating the compatibility matrix.",
  "scripts": {
    "create": "tsx src/create.ts",
    "submit-batch": "tsx src/submit-batch.ts",
    "query-status": "tsx src/query-status.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cosmjs/encoding": "^0.39.0",
    "@cosmjs/proto-signing": "^0.39.0",
    "@qorechain/rdk": "^0.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  }
}
