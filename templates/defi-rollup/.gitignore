node_modules/
.env
*.log

# Circuit build intermediates (r1cs, ptau, zkey stages). The small final
# artifacts under circuits/artifacts/ ARE checked in so the prover works
# out of the box.
circuits/build/
